/// <reference types="node" />
import { CreateSecp256k1InstructionWithEthAddressParams, TransactionInstruction } from "@solana/web3.js";
export declare const toBuffer: (arr: Buffer | Uint8Array | Array<number>) => Buffer;
/**
 * Create an secp256k1 instruction with an Ethereum address.
 *
 * We need to add an extra byte to the ethAddress to match our secp offset
 * */
export declare function createInstructionWithEthAddress2(params: CreateSecp256k1InstructionWithEthAddressParams): TransactionInstruction;
